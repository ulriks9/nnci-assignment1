N = 10;
alpha = [0.25, 0.5, 1.5, 2, 3, 4, 5, 6, 7, 8, 9, 10];
errors = zeros();

for a = alpha
    for n = 1 : n_D
        P = round(a * N);
        D = data(P, N);
        [~, error] = perceptron(D, a);
    end
    errors = [errors, error];
end

disp(alpha)

figure, hold on;
plot(alpha, errors)